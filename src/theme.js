import { createTheme } from "@mui/material";

export const theme = createTheme({
    palette: {
        type: 'light',
        primary: {
          main: '#027ee2',
        },
        secondary: {
          main: '#f4e177',
        },
        background: {
          default: '#f4e177',
        },
    }
  });

